/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ['icy-ghosts-wait.loca.lt', '*.loca.lt', '*.pinggy.link', '*.ngrok-free.app'],
};

export default nextConfig;
